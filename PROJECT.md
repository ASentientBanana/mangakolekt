


TODO:
 - Change the image loading strategy to dump images to fs to use for thumbnails (and maybe minify them) and load only the current images into memory. 
The current way is load them all into ram.