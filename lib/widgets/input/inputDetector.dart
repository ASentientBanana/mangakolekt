import 'package:flutter/material.dart';

//For mobile use gesture detector
//For desktop use Keyboard listener

class InputDetector extends StatelessWidget {
  const InputDetector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
